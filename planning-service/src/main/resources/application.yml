# ============================================
# SERVER CONFIGURATION
# ============================================
server:
  port: 8083

# ============================================
# DATASOURCE (MongoDB Atlas)
# ============================================
spring:
  application:
    name: planning-service
  data:
    mongodb:
      # Option 1 : URI unique (RECOMMANDÉ)
      uri: mongodb+srv://ahlemhorchani701_db_user:hd9L9KqaFGEylRhk@planning-cluster.o7nyftw.mongodb.net/planning_db?retryWrites=true&w=majority&appName=planning-cluster

# ============================================
# LOGGING
# ============================================
logging:
  level:
    com.soutenance.planning: DEBUG
    org.springframework.web: INFO
    org.springframework.data.mongodb.core: DEBUG
# ============================================
# swagger-ui
# ============================================

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operationsSorter: method
    tagsSorter: alpha
    display-request-duration: true
  api-docs:
    path: /v3/api-docs
    enabled: true
  show-actuator: true
  packages-to-scan: com.microservices.planning.controller